import { ScoreFlow } from "@/app/components/ScoreFlow";

export const metadata = {
  title: "Get your score — Stabil",
};

export default function ScorePage() {
  return (
    <main>
      <ScoreFlow />
    </main>
  );
}
